# 大乐透预测系统 - 智能依赖管理
# 支持云端部署和本地完整功能

# ===== 核心依赖（云端必需）=====
streamlit>=1.28.0
plotly>=5.15.0
pandas>=1.5.0
numpy>=1.24.0

# ===== 基础功能依赖 =====
requests>=2.31.0
beautifulsoup4>=4.12.0
scikit-learn>=1.3.0
scipy>=1.10.0
joblib>=1.3.0
tqdm>=4.65.0
PyYAML>=6.0

# ===== 可视化依赖 =====
matplotlib>=3.7.0
seaborn>=0.12.0

# ===== 系统监控 =====
psutil>=5.9.0

# 注意：移除了以下重型依赖以确保云端部署成功
# - tensorflow (版本冲突，Python 3.13不兼容)
# - torch (太大，编译复杂)
# - lxml (需要系统库)
# - pymc, arviz (贝叶斯分析，太重)
# - xgboost (可选，较大)

# 这些功能在本地环境中可以通过额外安装获得：
# pip install tensorflow torch xgboost pymc arviz lxml